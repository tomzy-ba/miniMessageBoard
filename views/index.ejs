<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <% messages.forEach(function(message) { %>
          <p>Message from <%= message.user %>: <%= message.text %> (added <%= message.added %>)</p>
    <% }); %>    <%= messages.forEach(function(message)) { %>
  </body>
</html>
